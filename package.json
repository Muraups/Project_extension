{
  "name": "projeto_extensao",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
      "predeploy": "rm -rf dist && mkdir dist && cp -r * dist", 
      "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
